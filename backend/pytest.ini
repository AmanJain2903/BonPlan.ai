[pytest]
testpaths = tests
pythonpath = .
addopts = --strict-markers --disable-warnings
markers =
    unit: isolated unit tests with mocked external services
    integration: FastAPI/application integration tests using mocked dependencies
